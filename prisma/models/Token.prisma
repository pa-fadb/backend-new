model Token {
    id                Int    @id @default(autoincrement())
    token             String @unique
    permissionsGrant  Int    @default(0)
    permissionsRevoke Int    @default(0)

    @@map("tokens")
}
